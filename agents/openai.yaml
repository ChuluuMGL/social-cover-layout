interface:
  display_name: "Social Cover Layout"
  short_description: "Route content briefs into platform-aware social covers"
  default_prompt: "Use $social-cover-layout to turn this finished content brief into an original, platform-ready cover plan or generated cover."

policy:
  allow_implicit_invocation: true
